# ── Secrets ───────────────────────────────────────────────
.env
.env.local
.env.production
frontend/.env.local
frontend/.env.production
frontend/.env.mobile

# ── Python ────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/

# ── Tools ─────────────────────────────────────────────────
.mypy_cache/
.pytest_cache/
.ruff_cache/

# ── Project-specific (per AGENTS.md) ─────────────────────
documents/raw/
node_modules/
backups/

# ── IDE ───────────────────────────────────────────────────
.vscode/
.idea/
*.swp

# ── Cache ────────────────────────────────────────────────
evaluation/cache/

# ── Terraform ─────────────────────────────────────────────
.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl
# Derived per-doc metadata sidecars — rewritten on every ingest (ingested_at,
# review_due, checksums, verifications). Write-only; nothing reads them at
# runtime, the corpus lives in the DB. catalog.json + registry.json stay
# tracked. Metadata source-of-truth is redesigned in agent architecture Phase 3.
documents/metadata/
