# Session 2026-07-12

## Accomplished

- **Migration 016** — `project_room_scans` table (structure + room rows, derived jsonb only)
- **DB client** — `list_project_room_scans`, `upsert_project_room_scans`, `set_active_room_scan`, mirrors active room to `projects.room_summary`
- **API** — `GET/POST /projects/{id}/room-scans`, `PATCH .../active`, `POST .../design-intent`
- **RAG** — `rag/project_context.py`, `generate_answer(project_context=...)`, query route loads context when `project_id` set
- **Design intent** — `rag/design_intent.py` rule fallback + Anthropic structured overlays
- **Frontend** — Filesystem storage, structure sync, master room list in `RoomScanPanel`, compliance cards, text/mic design intent
- **iOS plugin** — `startStructureCapture`, `openRoomAR`, `applyMaterial`, `startSpeechRecognition`; encoder v2.0 with surface IDs; `StructureCapturePresenter`, `RoomARPresenter`
- **Scripts** — `scripts/check_room_scans.py`
- **Tests** — `tests/test_project_room_scans.py`; frontend tests 50 pass

## Commit message

```
feat(room-scans): multi-scan structure/room RDS sync, RAG context, iOS AR design intent
```

## Prompt for next session

Read `STATE.md`, `AGENTS.md`, and this journal. Blocker: deploy migration 016 + backend to prod ECS before device multi-scan sync. Then device smoke: Scan House → 2 rooms → verify `check_room_scans.py` → Query with project selected mentions room metrics → Design in AR + mic intent. Android ARCore fallback still open.

---

## Session continuation — scan library + project dashboard UX

### Accomplished

- **Migration 017** — `user_room_scans` + `project_room_scan_links` (library-first model)
- **Backend** — user library CRUD (`/auth/me/room-scans`), project link/unlink APIs
- **Frontend** — Profile Room Scans page, home promo card, slim Projects list → `/projects/:id/dashboard`
- **Project dashboard** — nested layout: Dashboard, Room Scans, Query History, Documents, Members
- **LinkScansModal** — pick library scans to attach to a project
- **RoomScanPanel** — `libraryMode` for capture without project
- **Tests** — frontend 50 pass; `profileNavConfig` updated

### Commit message

```
feat(scans): user scan library, project dashboard UX, and link scans to projects
```

### Prompt for next session

Read `STATE.md`, `AGENTS.md`, and this journal. Apply migration 017 to prod, deploy backend with library routes, then device smoke: capture to library → link to project → query with active room. Run `npm run build:mobile && npx cap sync ios` before iPhone test.

---

## Session — Scan → Overlay → Real Product (Sprint 16)

### Accomplished

- **commerce/** — `product_resolver`, `serpapi_client` (HD + mock), `takeoff` (zip, qty, line total)
- **API** — design-intent returns enriched overlays + `product_candidates`; commerce search + materials estimate routes
- **Frontend** — `ProductCard`, `OverlayProductList`, `MaterialsEstimatePanel`; design intent shows shoppable SKUs
- **iOS AR** — `UnlitMaterial` texture from `product_ref.image_url`
- **Fix** — `saveRedesignOverlays` used wrong `scope` variable
- **Tests** — 5 commerce pytest + 50 frontend tests pass

### Commit message

```
feat(commerce): product-aware overlays, HD resolver, takeoff, and AR textures
```

### Prompt for next session

Read `STATE.md`, `AGENTS.md`, and this journal. Deploy migration 017 + commerce routes to prod. Set `SERPAPI_API_KEY` for live HD pricing. Device test: design intent → product card → AR texture → materials estimate on dashboard.
