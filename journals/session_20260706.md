# Session: 2026-07-06

## Type

Sprint 12 close — P1-5 kickoff fields on project detail.

## Goal

Finish last Sprint 12 deliverable: show wizard-collected fields (address, spaces, work types, recommended permits) on the Projects page after project creation.

---

## Completed

### Frontend
- `frontend/src/components/PermitTags.jsx` (new) — shared permit tag pills extracted from kickoff page
- `frontend/src/components/ProjectKickoffSummary.jsx` (new) — detail-panel kickoff summary block
- `frontend/src/projectKickoffSummary.js` (new) — `formatKickoffSummary()` helper for nullable field display
- `frontend/src/projectKickoffSummary.test.js` (new) — 4 unit tests (empty, partial, full kickoff data)
- `frontend/src/ProjectsPage.jsx` — renders `ProjectKickoffSummary`; sidebar shows address under project name
- `frontend/src/ProjectKickoffPage.jsx` — imports shared `PermitTags`
- `frontend/src/styles.css` — `.project-kickoff-summary`, `.project-kickoff-empty`, `.project-list-item-text`

### Validation
- `npm run test` — 27 pass, 0 fail (includes 4 new `formatKickoffSummary` tests)

### Docs
- `STATE.md` — Sprint 12 closed; P1-5 checked off; next tasks updated
- `README.md` — Sprint 12 kickoff wizard moved to Completed

---

## Sprint 12 status

**CLOSED ✅** — all 10 deliverables complete.

---

## Git commit message

```
feat(sprint12): show kickoff wizard fields on project detail
```

## Prompt for next session

Read `AGENTS.md`, `STATE.md`, and `journals/session_20260706.md`. Sprint 12 is closed. Launch is blocked on deploy + prod P0 verification only (registration, queries, Mapbox, `/projects` refresh, `/api/documents`). First step: `terraform apply` → deploy backend + frontend → CloudFront invalidation. Then run prod smoke checklist in STATE.md. Optional cleanup: delete Cognito user `uxaudit_user1` and test project `22cd04d1-a1b8-4b8f-b44a-5287ed09c0bc`.
