# Session 2026-07-23 — Agent architecture plan + Phase 0 (trace store)

## What was accomplished

### Planning
- Wrote [docs/agent_architecture.md](../docs/agent_architecture.md): 26 agents +
  2 services under one Manager, across 10 phases with explicit tradeoffs per phase.
  Covers prompt routing by persona, a Bid Evaluator, a Corpus Metadata Validator,
  a feedback/action-queue loop on a superadmin dashboard, per-agent autonomy
  levels, a measured cost model, and where ReAct does and does not apply.
- Supersedes the two `.gemini` brain plans (agent implementation + token
  optimization); README Planned entries updated to point at the new doc.

### Phase 0 — trace store
- `db/migrations/026_agent_traces.sql` — five tables. Policy autonomy ceilings
  are seeded as data, not code, straight from AGENTS.md governance rules.
- `db/client.py` — trace helpers, action-queue upsert deduped by a partial unique
  index on open items, `set_agent_autonomy` clamped to `max_level` **in SQL** so a
  direct API call cannot exceed a ceiling the dashboard merely hides.
- `audit/logger.py`, `audit/provenance.py`, `audit/anomaly.py` — all three were
  0-byte stubs; now implemented.
- Retrofitted `@traced` onto `generate_answer` and `_parse_design_intent_llm`, and
  a run scope onto `query_answer` via a decorator (the handler is 377 lines; a
  `with` block would have meant reindenting all of it).
- 22 new tests. Full suite green at 292 passed.

## Findings worth keeping

1. **Reranker-rejected chunks were being prompted and billed.** `reranker.py:100`
   marks them `filtered_out=True` and `retriever.py:243` exposes `passing_chunks`
   for exactly this, but `query.py:423` passed `result.chunks`. Fixed inside
   `generate_answer` so the eval harnesses (`ragas_eval.py:652`,
   `langsmith_eval.py:123`) get it too — they had the same bug, meaning evals were
   scoring a different chunk set than intended.
2. **`GenerationResult` discarded its cache token counts.** They were computed at
   `generator.py:464`, logged, and thrown away — so cache effectiveness, a headline
   metric of the plan, was unmeasurable. Now carried on the dataclass under the
   Anthropic field names.
3. **Two sprint8 tests were failing before this session.** `MagicMock.__iter__`
   returns an *empty* iterator while the attribute itself is truthy, so
   `body.chunk_ids` sent the route down the by-id branch and the patched retrieval
   never ran. They also patched `api.routes.query.retrieve`, which `/query/answer`
   does not call — it calls `retrieve_with_project`. Both fixed.
4. **`_extract_citations` keeps unverifiable citations**, flagged
   `found_in_context=False`, and nothing currently blocks them from reaching the
   user. Pre-existing and left alone — that is the Citation Verifier's job (agent
   #9, Phase 5). Documented in the test.
5. **Sonnet 5 intro pricing is live** ($2/$10 vs $3/$15 through 2026-08-31), so
   real spend today is ~⅓ below the plan's list-price table. Encoded with an
   expiry date so it self-corrects on 2026-09-01.
6. **Dedupe bug caught by a rolled-back dry run of migration 026.** The action
   queue's unique index covered nullable `entity_type`/`entity_id`, and NULLs
   never conflict in a unique index — so any entity-less item (exactly what a
   scheduled anomaly sweep files) skipped dedupe silently: 2 rows from 2 upserts.
   Fixed by making both columns `NOT NULL DEFAULT ''`. An expression index over
   `COALESCE()` was tried first and rejected: Postgres cannot infer a *partial*
   index from an expression `ON CONFLICT` (`InvalidColumnReference`).
   **This is the argument for verifying before deploying** — AGENTS.md freezes a
   migration once deployed, so applying 026 to prod first would have required a
   027 to correct it.

## Verification performed

Migration 026 was dry-run against local Docker Postgres inside
`conn.transaction(force_rollback=True)` — nothing persisted. Confirmed: all five
tables create, 15 autonomy ceilings seed, entity-bearing dedupe collapses 3
upserts to 1 row, entity-less dedupe collapses 2 to 1, distinct entities stay
separate, and a resolved item can be legitimately re-raised.

**Phase 0 verified end to end on local** via `py scripts/verify_phase0.py --local`:
schema, autonomy enforcement, corpus, and the trace store all pass. The trace
store check is the one that matters — it writes a run + step row with non-zero
`cost_usd` through the same instrumentation the API route uses.

## Cross-machine work (second half of session)

The corpus lives on a second machine, which surfaced a class of problems that
had nothing to do with Phase 0 and everything to do with not knowing which
database a script was talking to.

7. **Migration 026 was applied to production RDS by accident.**
   `bootstrap_env` loads `.env` last with `override=True`, and
   `ENVIRONMENT=production` selects `.env.production`; all three dotenv files are
   gitignored, so the target differs per machine. `apply_migration.py` printed
   "Migration applied successfully." identically whether it hit Docker or RDS.
   Because AGENTS.md freezes a deployed migration, the dedupe correction had to
   ship as **027** rather than an edit to 026.
8. **`.env.local` does not reliably mean localhost.** On machine B it points at a
   campus IP left over from remote debugging, so `--local` honoured the file but
   still resolved off-box. The first version of the banner then told the user to
   "re-run with --local" — advice they had just followed. Fixed: the banner
   reports the real host *and* the file that supplied it.
9. **Two migrations were numbered 026** (`026_agent_traces.sql` and
   `026_design_intent_usage_project_fk.sql` from `dc802a9`). Recorded rather than
   renamed — both are already applied by name on multiple databases, and renaming
   an applied migration is riskier than the duplicate.
10. **`init_rds_db.py` had an inverted safety check** — it prompted only when the
    target *was* localhost, so `DROP SCHEMA public CASCADE` against RDS ran with
    no confirmation at all. Now any non-local target must have its hostname typed
    back.
11. **`ingest_documents.py` and `backfill_source_identity.py` had no target
    visibility**, so a corpus could be written to the wrong database silently.
    Both are now target-aware; the backfill also names missing migration 022 as
    the cause instead of dying on `UndefinedColumn`.
12. **`verify_phase0.py` replaced a curl-based check.** The original suggestion
    was bash `curl` against `/api/query/answer`, which fails twice on the second
    machine: PowerShell aliases `curl` to `Invoke-WebRequest`, and the route
    needs a real Cognito token. The script exercises the same instrumentation
    with no server, no token, and no shell-syntax differences. Writing it
    reproduced the user's exact `UndefinedColumn` error, which is now a reported
    check rather than a crash.

New shared module `scripts/_db_target.py` holds target resolution (`--local`,
`--database-url`, banner, fail-fast reachability) for every diagnostic.

## Decisions

| Decision | Choice |
|----------|--------|
| Structured outputs | Native `client.messages.parse()`, not `instructor` — pydantic is already a dep |
| Trace table types | `text` + `CHECK`, not enums — new agent names never need `ALTER TYPE` |
| Tracing failure mode | Degrade to a log line, never raise |
| Autonomy ceilings | Clamped in SQL *and* the runtime; dashboard cannot raise them |
| Persona default | `research`, never `diy` |
| Run scope on the route | Decorator, not a `with` block — avoids a 377-line reindent |

## Commit

```
feat: agent architecture plan + Phase 0 trace store, drop filtered chunks from prompts
```

## Still open at session end

- **RAGAs not re-baselined.** Blocks the prod push: the chunk-leakage fix changes
  production answers, and AGENTS.md requires RAGAs after any pipeline change.
- **Prod has 026 in its pre-fix form** and needs 027.
- `ingestion/embedder.py` and `scripts/run_chunk_verify.py` are still not
  target-aware; they follow default dotenv resolution.

## Prompt for next session

> Read STATE.md, journals/session_20260723.md, AGENTS.md, and
> docs/agent_architecture.md.
>
> First: apply migration 026 locally, run one `/query/answer`, and confirm rows
> land in `agent_runs` + `agent_steps` with non-zero `cost_usd`. Then re-baseline
> RAGAs — the chunk-leakage fix changes what the model sees, so faithfulness will
> move and that is expected, not a regression. Capture the new baseline before
> Phase 2 relies on it as a no-change gate.
>
> Then Phase 1: `rag/agent_runtime.py` as the single Anthropic call site
> (`messages.parse` structured outputs, model ladder, prompt caching, token
> accounting, retries, autonomy enforcement, automatic tracing) plus
> `rag/agents/registry.py` with the `AgentSpec` protocol. Do not add new agents
> yet — Phase 2 ports the existing query chain behind the Manager with zero
> behaviour change, and that only proves anything if the runtime lands first.
