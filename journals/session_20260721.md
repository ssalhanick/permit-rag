# Session 2026-07-21 — Prod corpus ingest + URL pull plan

## What was accomplished

- Diagnosed prod ingest failure: RDS SG only allowed campus IP `129.110.242.97/32`;
  home IP blocked → 30s pool timeout ("Is Docker running?" was a red herring)
- Added home IP `68.20.22.215/32` to terraform RDS ingress CIDR list; applied successfully
- Ran `scripts/ingest_prod_corpus.py` against production:
  - Ingest: 20 succeeded, 2 skipped (mansfield/parker not in catalog)
  - Embed: 19/19 already embedded
  - Final counts: **19 active docs, 17,159 chunks, all embedded**
- Fixed `_verify_counts` in `ingest_prod_corpus.py` — `fetchone()[0]` KeyError
  under `dict_row`; now uses `COUNT(*) AS n` + `row["n"]`
- Wrote [docs/on_demand_url_pull.md](../docs/on_demand_url_pull.md):
  identity model (normalized URL + filename fallback), migration 022,
  crawler, pull route, UploadPage UI, SSRF guards, build order
- Linked plan in README Planned section

## Decisions

| Decision | Choice |
|----------|--------|
| Doc identity | Normalized URL primary; `municipality+doc_type+filename` fallback + review flag |
| Supersede timing | Only after successful chunk + embed |
| Pull trigger | Manual admin Pull button only — no page watcher |

## Blockers / Issues encountered

- PowerShell `2>&1 | Tee-Object` turns Python stderr INFO into `NativeCommandError` — ignore
- Harvest SSL/404 failures for a few catalog URLs (Dallas fee PDFs, Frisco, OSHA) — local raw reused where present
- `mansfieldtx-tx-1` / `parker-tx-1` in raw but not catalog — skipped

## Commit

```
docs: session wrap — prod corpus state and URL pull next-session prompt
```

## Branch

`deployment/sites`

## Prompt for next session

> Read STATE.md, journals/session_20260721.md, AGENTS.md, and docs/on_demand_url_pull.md before starting.
>
> Last session: Prod RDS corpus is seeded (19 docs / 17,159 embedded). Terraform SG allows home + campus IPs. Plan for on-demand URL pull is written and linked in README.
>
> Next session tasks (build order from the plan):
> 1. Prerequisite fixes — rewrite `governance._rechunk_and_embed` to match upload pipeline; fix `upload.py` missing `db_client` import; reconcile upload enum lists with `schema.sql`.
> 2. Implement `ingestion/url_normalize.py` + `tests/test_url_normalize.py`.
> 3. Add migration `022_source_identity.sql` + backfill.
> 4. Then page crawler → pull route → UploadPage UI.
>
> Do not start coding beyond prereqs until those three bugs are green.
