# Session: 2026-07-09

## Type

Sprint 13 close — prod deploy, mobile Phase 0 device verify, push crash fix.

## Goal

Apply migration 015, deploy Sprint 13 to prod, Cognito mobile setup, run Phase 0 gates, close STATE for Sprint 14.

---

## Completed

### Infra + DB
- Migration **015** (`room_summary` jsonb) applied local (Docker) + RDS
- `terraform apply` for mobile CORS — created task def `:10` with **wrong** RDS password (rolled back to `:9`, then deployed `:11` with correct creds + mobile CORS)
- GHA `deploy.yml` pushed Sprint 13 backend + frontend to `deployment/sites`
- ECS stable on **`permit-rag-backend:11`**

### Cognito + mobile env
- Mobile callback URLs in Cognito app client
- Sign in with Apple IdP + web browser test → kickoff screen ✅
- `frontend/.env.mobile` filled; `build:mobile` + `cap sync`

### Phase 0 gates
- M0-1–M0-5, M0-8, M0-9 verified
- M0-6/M0-7 deferred (no device Google/Apple password handy; web Apple OK)
- Android Studio: email login, project create, RAG query on physical device ✅

### Bug fixes
- `pushNotifications.js` — gate `register()` until `VITE_PUSH_NOTIFICATIONS_ENABLED=true` (Firebase crash on launch)
- `sprint12_p0_smoke.ps1` — accept `status: healthy`; `-UseBasicParsing` on web requests

### Docs
- `STATE.md` — Sprint 13 closed, Sprint 14 room capture next
- `docs/mobile_phase0_gates.md` — gate statuses updated
- `README.md` — Sprint 13 completed, Sprint 14 planned

---

## Lessons

- **Never bare `terraform apply`** for ECS until `DATABASE_URL` in terraform matches live RDS (or use SSM).
- **GHA** preferred for backend/frontend deploy; terraform infra separate.
- **Push plugin** requires `google-services.json` before calling `PushNotifications.register()`.

---

## Git commit message

```
fix(mobile): gate push init until Firebase; close Sprint 13 state and Phase 0 gates
```

---

## Prompt for next session

Read `AGENTS.md`, `STATE.md`, and `journals/session_20260709.md`. Sprint 13 closed. Sprint 14: wire room capture per `docs/sprint_14_3d-room-capture-agnostic-guide.md` — implement `startCapture()` Capacitor plugin boundary, interchange JSON v1.0, on-device metrics → `PATCH /api/projects/{id}/room-summary`. iOS RoomPlan first; Android ARCore/manual fallback. Optional backlog: OAuth deep links (M0-6/M0-7), terraform RDS password / SSM, Firebase push.
