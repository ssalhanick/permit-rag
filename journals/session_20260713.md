# Session 2026-07-13

## Accomplished

- **Migration 018** — `design_intent_usage` table for per-user/project/room_scan token logging
- **API** — scan_id design-intent routes; shared helpers; soft monthly cap
- **Frontend** — `RoomDesignPage` Preview/Save/history/branch/AR/DXF; AR voice dictation
- **iOS** — `active_revision_id` overlays; UUID case alignment; Dictate Command
- **Chunk status fix** — `db/client.py` uses `c.status` (not `c.chunk_status`); pipeline/ragas load `.env.local` via `bootstrap_env`
- **A: AR product textures** — `applyOverlaysToAR` passes product + asset URLs; iOS loads remote + local file textures
- **B: Generative room image** — `commerce/room_image.py`; `POST /api/commerce/room-preview-image`; optional Camera photo; saves `generated_preview.png`; stamps `overlay.asset_url`; UI Generate image; AR prefers asset_url
- **ECS deploy fix** — Dockerfile now `COPY commerce/`; hatch packages + GHA path filter include `commerce/**` (was crash-looping: `ModuleNotFoundError: commerce`)
- **Tests** — pytest room image (5) + frontend stamp/api tests pass
- **Docs** — `docs/room_generative_preview.md`; README/STATE updated

## Commit message

```
fix(deploy): include commerce package in Docker image so ECS can stabilize
```

## Prompt for next session

Read `STATE.md`, `AGENTS.md`, and this journal. Push Dockerfile/commerce packaging fix and re-run GHA deploy on `deployment/sites`. Confirm ECS tasks stay RUNNING. Then device smoke Generate image. Set `OPENAI_API_KEY` only if live images needed.
