# Session 2026-07-13

## Accomplished

- **Migration 018** — `design_intent_usage` table for per-user/project/room_scan token logging
- **API** — `POST /projects/{id}/room-scans/{scan_id}/design-intent`; `POST /auth/me/room-scans/{scan_id}/design-intent`; shared `api/design_intent_helpers.py`; soft monthly cap
- **RAG** — `parse_design_intent` returns `usage` (input/output tokens + model)
- **Frontend** — `RoomDesignPage` (Preview/Save, history, branch, AR, DXF export); routes for project + profile library
- **Device storage** — `designHistory.js` + `redesign.json` v2 revisions; v1 migration on read
- **DXF** — `roomCadExport.js` walls + overlay layers; Capacitor Share on native
- **UX** — Scan Single Room primary; Scan House demoted; post-scan → design nav; standalone scan `structureId` fix
- **iOS** — `RoomARPresenter` reads `active_revision_id` overlays, performs case-insensitive discoverCapture searches, and implements native "Dictate Command" button for live voice dictation
- **UUID Case Alignment** — Fixed case mismatch of UUID paths on iOS by lowercasing Swift UUIDs and implementing case-insensitive lookup fallback in JS `roomScanFilesystem.js`
- **AR Live Voice** — Added `onARSpeechCommand` Capacitor listener in `RoomDesignPage.jsx` to process native dictation transcript through RAG/LLM preview and automatically apply overlays inside the active AR viewer session
- **Tests** — 6 pytest + 60 frontend tests pass

## Commit message

```
feat(ar): live voice dictation commands from within the native AR viewer
```

## Prompt for next session

Read `STATE.md`, `AGENTS.md`, and this journal. Run `cd frontend && npm install && npm run build:mobile && npx cap sync ios` before iPhone test. Device smoke: Scan Single Room → Preview → Save → branch → AR → Tap "Dictate Command" inside AR → verify live material overlay → Export DXF. Apply migration 018 to prod RDS and deploy backend with scan_id design-intent routes. Set `DESIGN_INTENT_MONTHLY_TOKEN_CAP` in prod if needed.
