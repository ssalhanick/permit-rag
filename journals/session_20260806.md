# Session 2026-08-06 — LangSmith agent-eval wiring, bookkeeping close-out

Continuation session on `feat/langsmith-agent-eval` (5 commits on top of
`deployment/sites`, confirmed pushed to origin at session start). This
session's own work was documentation-only (AGENTS.md post-session protocol
was never run for the branch's actual coding work, done in an earlier
session) plus a code-level root-cause pass on one open finding. No commits
made this session pending Scott's merge decision.

## What shipped on this branch (prior session, recorded here for the first time)

1. `b906bbb` — LangSmith wired into `evaluation/agent_eval.py` via a new
   `Check.gates` flag: LangSmith-derived checks default `gates=False`
   (advisory/report-only), existing Postgres/RAGAs checks keep `gates=True`.
   New advisory metrics: hallucination judge + citation precision/recall on
   `answer_generator`, project-isolation pass rate on `manager`.
2. `9b289ff` — `hallucination_judge` in `evaluation/langsmith_eval.py` now
   routes through `rag/agent_runtime.py` (was an inline `anthropic.Anthropic()`
   call — an AGENTS.md single-call-site violation). Every `langsmith_eval.py`
   experiment now tagged with both `prompt_version` and `library_version` in
   metadata.
3. `148b870` — `scripts/seed_langsmith_security_fixture.py` (first version,
   had two bugs, fixed next commit), `permit_rag_security_v1.json` pointed at
   the fixture, `docs/langsmith_eval_ui_guide.md` written.
4. `9d029a1` — two real bugs found by actually running the seed script: (a)
   `documents.project_id` has a real FK against `projects` — can't use an
   invented UUID, now uses `get_or_create_cognito_user` + `create_project`
   looked up by a fixed name for idempotency; (b) the fixture was
   `visibility='private'`, but `langsmith_eval.py` never passes
   `requesting_user_id` (always `None`), so a private tier-3 doc is excluded
   unconditionally regardless of `project_id` — the isolation test would have
   passed vacuously. Switched to `visibility='team'`.
5. `2c4a293` — two more real bugs found by running an actual experiment and
   checking real scores via the API: (a) `Project.feedback_stats` is never
   populated for `evaluate()`-run experiments (confirmed empirically — both
   `list_projects()` and a forced `read_project()` returned `None`); real
   scores only exist via `list_feedback(run_ids=...)`, now aggregated by hand
   in `_aggregate_feedback()`; (b) the staleness check treated a *missing*
   `library_version` tag as "no claim, don't penalize" — backwards, since it
   let a 2026-07-22 baseline experiment's stale numbers through (it predates
   version tagging entirely). Now fails closed: missing tag = can't verify =
   don't trust.

839 tests passing, ruff clean, as of `2c4a293`.

## What's actually been verified for real (not mocked), on Machine B

- Seed script runs correctly and is idempotent (re-run → `num_new: 0`).
- Security dataset (`permit_rag_security_v1`): fresh experiment run,
  `project_isolation = 1.0` confirmed via a direct API check of real per-run
  feedback (`list_feedback`), not an assumed aggregate. This is real
  verification of migration 045's cross-tenant document-isolation fix —
  STATE.md punch item #5 (below) is updated to reflect this.
- Eval dataset (`permit_rag_eval_v1`): fresh experiment run
  (`verify-fix-7da1af15`), confirmed tagged with both `prompt_version=v3`
  and `library_version=lib-873aecbe24cb` — proves the staleness-detection
  tagging works end to end, not just in unit tests.
- `scripts/run_agent_eval.py --days 7` and `scripts/review_feedback.py
  --dry-run` both run cleanly against Machine B's local corpus DB.

**Caveat that must travel with all of the above:** everything ran against
Machine B's *local* Postgres (dev/test traffic only). Real production
traffic writes to prod RDS, not this DB. These runs prove the *code* works —
they do not describe how the agents perform for real users. The next real
evaluation needs prod RDS (read-only) or accumulated real trace volume — a
distinct, not-yet-done step (see Next tasks).

## Today's work: root-causing the query_deconstructor 100% error rate

Machine B's `scripts/run_agent_eval.py` output showed `query_deconstructor`
at `det=0.00 err=1.00`. Investigated by reading code only (this machine's
local DB is empty, per the two-machine split — nothing here could be run
against real data).

Finding: `deconstruct()` in `rag/agents/deconstructor.py` is deliberately
designed to never raise — any `run_agent()` failure is caught and it
degrades to the single-question form, invisible to the Manager and the user
(`_deconstruct` in `manager.py` has its own second layer of the same
try/except). But `run_agent()` itself (`rag/agent_runtime.py:360-367`)
records a `status="error"` row to `agent_steps` for `agent_name=
"query_deconstructor"` *before* the exception is re-raised and caught by
the caller. So a 100% error rate here means every recorded model-call
attempt for this agent in the window failed at the API level — the
graceful-degrade design means this is completely invisible in the live
product, but the telemetry is still catching something real underneath.

Also relevant: the cheap deterministic path (`_looks_compound()` returns
false) never calls `run_agent()` at all, so it never writes an `agent_steps`
row — meaning `query_deconstructor`'s `deterministic_rate` in the scorecard
structurally reads near-zero regardless of how often the free path actually
fires, because only the LLM-invoking branch is ever visible to
`agent_scorecard()`. Separately, `query_deconstructor` has no entry in
`evaluation/agent_eval.py`'s `CONTRACTS` dict — it only inherits
`GLOBAL_CHECKS` (`error_rate ≤ 0.05`), which is what's tripping here.

**Not yet distinguished, needs Machine B:** whether the underlying model-call
failures are an environment/credential issue (an invalid `ANTHROPIC_API_KEY`
was found and is recorded as a known issue on a different session/branch,
2026-08-01 — unconfirmed whether it's the same cause here or already fixed)
or a real bug specific to this call (e.g. the `Deconstruction` structured
output schema, `Tier.CHEAP` + `effort="low"` combination). Recommend on
Machine B: pull a few of the actual `agent_steps.error` messages for
`query_deconstructor` in the window directly (`SELECT error FROM
agent_steps WHERE agent_name='query_deconstructor' AND status <> 'ok'`) —
that will settle it in one query rather than more code reading.

## Design question surfaced, not resolved

`answer_generator`'s LangSmith advisory metrics may never display even
though the fetch succeeds: `agent_eval.py`'s `compute_metrics()` only emits
a report row for agents present in `db_client.agent_scorecard()`'s result
for the window. Zero scorecard rows for `answer_generator` in a sparse
window (plausible on local data) means the LangSmith advisories are computed
but never surfaced. Open question, not decided: is that correct (LangSmith
metrics complement live-traffic rows, not conjure new ones) or a gap worth
fixing.

## Next session

1. On Machine B: run the one-query check above to settle the
   query_deconstructor root cause before deciding whether it blocks merge.
2. Get Scott's go-ahead, then merge `feat/langsmith-agent-eval` →
   `deployment/sites` and push.
3. Decide the path to real (prod-reflective) agent performance data — prod
   RDS read-only access vs. waiting for real trace volume — still unresolved,
   was the original ask behind this whole branch.
4. Unrelated, still open: multi-sample live RAGAs baseline (STATE.md punch
   #3b, still one sample); `feat/citation-deep-link` is deferred until Scott
   creates that branch, and fixing `citation_precision`/`citation_recall` in
   the LangSmith harness waits on it per his sequencing.
