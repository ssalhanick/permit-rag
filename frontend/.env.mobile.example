# Mobile build — copy to .env.mobile.local for local device testing
VITE_API_BASE_URL=https://permits.scottsalhanick.com
VITE_MOBILE_OAUTH_REDIRECT_URI=com.scottsalhanick.permitrag://auth/callback
# Mapbox intentionally omitted on mobile (manual address on kickoff)
# VITE_MAPBOX_TOKEN=

# Cognito — same pool as web; register mobile callback URLs in Cognito console
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_APP_CLIENT_ID=
VITE_COGNITO_DOMAIN=
